
{{ #each posts as |post| }}
<div>
  <div>
    <h2>
      <a href="/post/{{post.id}}">{{post.title}}</a> 
    </h2>
    <p>
        Posted by {{post.user.name}} on {{format_date project.date_created}} 
    </p>
  </div>
  <div>
    <p>
      {{post.content}}
    </p>
  </div>
</div>
{{/each}}
